# ── Core ML ──────────────────────────────────────────────────────────────────
pandas>=2.2.3
numpy>=2.1.0
scikit-learn>=1.6.0
xgboost>=2.1.4
imbalanced-learn>=0.13.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.9.2
seaborn>=0.13.2

# ── Utilities ─────────────────────────────────────────────────────────────────
joblib>=1.4.2
tqdm>=4.66.4
pyarrow>=17.0.0             # Parquet file support

# ── Scalable Pipeline (optional — needed for scalable_pipeline.py) ────────────
kafka-python>=2.0.2        # Apache Kafka client
redis>=5.0.4               # Redis in-memory connection tracker
requests>=2.32.3           # HTTP client for model serving
boto3>=1.34.100            # AWS Shield / WAF integration

# ── REST API (FastAPI) ─────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0

# ── Packet Capture (Live Mode) ─────────────────────────────────────────────────
scapy>=2.6.1